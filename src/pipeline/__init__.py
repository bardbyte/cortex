"""Pipeline — orchestration layer for Cortex.

Currently: retrieval is handled directly via src/retrieval/pipeline.py.
The agent orchestration layer (Looker MCP ReAct loop) will be rebuilt here
once the retrieval foundation is validated at 90%+ accuracy.
"""
